# Speech stack for Zer (open source).
#
# Speech-to-text — OpenAI Whisper on CTranslate2 (CPU-friendly).
faster-whisper>=1.0,<2

# Text-to-speech:
#   MMS-TTS (Meta neural VITS) — the strong Amharic voice; needs torch+transformers.
#   Piper — fast, natural English ONNX voices.
# Torch is installed CPU-only in the Dockerfile (smaller, no CUDA); transformers
# drives MMS here. Uncomment trl separately if you want SFTTrainer.
transformers>=4.44
sentencepiece
piper-tts>=1.2
