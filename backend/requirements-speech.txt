# Speech stack for Zer (open source).
#
# Speech-to-text — OpenAI Whisper on CTranslate2 (CPU-friendly).
faster-whisper>=1.0,<2

# Text-to-speech:
#   MMS-TTS (Meta neural VITS) — the strong Amharic voice. It needs `uroman`
#   because MMS expects romanized text for non-Latin scripts.
#   Piper — fast, natural English ONNX voices.
# Torch is installed CPU-only in the Dockerfile (smaller, no CUDA).
# transformers is pinned below 5.x: the VITS path is stable on 4.4x.
transformers>=4.44,<5
sentencepiece
uroman>=0.2
piper-tts>=1.2

# High-quality resampling for the speed control (band-limited, no aliasing),
# so changing the speaking rate does not make the neural voice sound metallic.
soxr>=0.3
