# Zer fine-tuning environment (install on the GPU machine).
# GPU (CUDA) install:  pip install -r training/requirements-train.txt
# CPU-only (smoke test): pip install torch --index-url https://download.pytorch.org/whl/cpu
#                        then the rest below except bitsandbytes/trl.

torch>=2.2
transformers>=4.44
peft>=0.12
accelerate>=0.33
datasets>=2.20
bitsandbytes>=0.43        # 4-bit QLoRA (CUDA/Linux); omit on CPU or macOS
trl>=0.11                 # optional: SFTTrainer alternative
sentencepiece
safetensors
